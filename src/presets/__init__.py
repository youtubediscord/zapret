"""Preset mode package.

Корневой пакет должен оставаться тонким: без раннего импорта runtime,
service и других тяжёлых модулей. Иначе любой импорт подмодулей сначала
выполнит этот файл и может затянуть верхний слой приложения обратно в
низкоуровневые модули.
"""
